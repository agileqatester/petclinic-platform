# ADR-0019 / ADR-0024. One replica. HPA and PDB off.
# Published limits still do not admit under the ADR-0023 quota. Not installed.
namespace: petclinic-dev
image:
  env: dev
replicaCount: 1
autoscaling:
  enabled: false
podDisruptionBudget:
  enabled: false
